<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etlpat.mapper.SeckillVoucherMapper">

    <resultMap id="BaseResultMap" type="com.etlpat.pojo.SeckillVoucher">
        <id property="voucherId" column="voucher_id"/>
        <result property="stock" column="stock"/>
        <result property="createTime" column="create_time"/>
        <result property="beginTime" column="begin_time"/>
        <result property="endTime" column="end_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        voucher_id
        ,stock,create_time,begin_time,end_time,update_time
    </sql>
    <update id="updateStockByVoucherIdAndStock">
        update tb_seckill_voucher
        set stock = #{stock,jdbcType=NUMERIC}
        where voucher_id = #{voucherId,jdbcType=NUMERIC}
          AND stock = #{oldStock,jdbcType=NUMERIC}
    </update>
    <update id="deductStock">
        UPDATE tb_seckill_voucher
        SET stock = stock - 1
        WHERE voucher_id = #{voucherId}
          AND stock > 0
    </update>

</mapper>
